interface ContactInfo {
  label: string;
  value: string;
}

interface Store {
  name: string;
  address: string;
}

interface FooterLink {
  name: string;
  href: string;
}

interface SocialMedia {
  name: string;
  icon: string;
  href: string;
}

interface PaymentMethod {
  name: string;
  icon: string;
}
